---
status: accepted
---

# 先模块化单体，再拆进程

学习目标包含 Spring Cloud，但当前没有已在跑的多个服务，也还没有稳定的订单主路径。一上来按模块各起一个进程（注册中心、网关、远程调用）会把「学会拆分」和「把交易做对」绑在一起，两边都难验收。

因此：v1 **一个进程、多个模块**，模块边界按 [MODULES.md](../MODULES.md) 当成将来的服务边界来守（所有权、依赖方向、不互相写库）。等「多行下单 + 支付 + 库存预占」稳定之后，再把模块搬成独立部署单元。
